# Core (required)
fastapi>=0.110,<0.115
uvicorn[standard]>=0.23
httpx>=0.27
pydantic>=1.10,<3.0

# Optional (recommended)
# Enables community detection in /v1/graph endpoints (insight_router); router degrades gracefully if absent.
networkx>=3.1

# Faster JSON responses when available; FastAPI auto-detects.
orjson>=3.9

